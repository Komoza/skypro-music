import * as S from './compilation.style';

export const Compilation = () => {
    return (
        <S.compilation>
            Страница&nbsp;<S.compilationText>подборка</S.compilationText>
        </S.compilation>
    );
};
